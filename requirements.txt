# Python dependencies for repo-local helper scripts and launch-time YAML parsing.
# ROS 1 Noetic Python modules such as rospy, roslaunch, sensor_msgs, and cv_bridge
# are provided by ROS apt packages / rosdep, not by PyPI.
PyYAML>=5.4
numpy>=1.21,<2.0
opencv-python>=4.5,<4.12
